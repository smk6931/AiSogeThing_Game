# 2026-04-06 Next Plan

## 오늘 한 작업

- `ComfyUI`를 [tools/ComfyUI](/C:/GitHub/AiSogeThing_Game/tools/ComfyUI) 에 설치했다.
- `ComfyUI` 전용 Python 가상환경 `.venv`를 만들고 기본 의존성을 설치했다.
- CUDA용 `torch 2.11.0+cu128` 설치를 확인했다.
- 로컬 GPU 인식 확인:
  - `cuda = True`
  - `device = NVIDIA GeForce RTX 4060 Laptop GPU`
- 게임 월드 쪽에서는 레이어 토글 최적화를 진행했다.
- 기본 플레이 레이어를 `지도 + 도로 + 그룹선 + 그룹텍` 기준으로 조정했다.
- `CityBlockOverlay` 쪽에 `group_key` 기준 텍스처 오버라이드 테스트 코드를 넣었다.

## 현재 상태

- `ComfyUI` 본체 실행 환경은 준비 완료.
- 아직 실제 생성용 모델 체크포인트는 정상 다운로드 완료되지 않았다.
- SDXL base 다운로드 시도 중 Hugging Face 연결이 끊겼다.
- 부분 다운로드 파일이 남아 있다:
  - `tools/ComfyUI/models/checkpoints/sd_xl_base_1.0.safetensors`
  - 현재 크기: 약 `2.58 GB`
- 이 파일은 완전한 체크포인트가 아닐 가능성이 높다.

## 내일 해야 할 작업

1. `sd_xl_base_1.0.safetensors` 부분 파일 상태 확인 후 삭제 또는 이어받기 전략 결정
2. SDXL 체크포인트 정상 다운로드 완료
3. `ComfyUI` 실행 테스트
4. 기본 `txt2img` 워크플로 1개 저장
5. 프로젝트용 입력/출력 폴더 구조 만들기
6. `group polygon mask -> generated texture`용 최소 워크플로 설계
7. `seoul.dongjak.noryangjin1.group.g07` 테스트 그룹에 마스크 기반 생성 이미지 연결 재시도

## 추천 다음 순서

1. 모델 다운로드 문제부터 해결
2. `ComfyUI` 단일 이미지 생성 성공 확인
3. 폴리곤 마스크 PNG 추출 스크립트 작성
4. 그룹별 텍스처 생성 파이프라인 연결

## 메모

- 현재 목표는 `타일 반복`보다 `group polygon 맞춤형 바닥 이미지` 생성이다.
- 최종 방향은 `group_key 기반 커스텀 텍스처`가 맞다.
- 판정은 micro partition, 시각 표현은 grouped play region 중심으로 유지하는 방향이 적절하다.
