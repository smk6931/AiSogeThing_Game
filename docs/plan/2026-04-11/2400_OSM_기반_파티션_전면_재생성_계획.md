# OSM 실제 데이터 기반 파티션 전면 재생성 계획 (v2)

**작성일**: 2026-04-11  
**최종 실행**: 2026-04-12 (노량진1동 완료)  
**상태**: 노량진1동 완료 / 노량진2동 대기

---

## 배경 및 목표

현재 파티션은 손으로 그린 "Sector Plot" 임의 폴리곤을 도로로 자른 것.  
→ OSM 실제 지형 feature(공원, 강, 건물군, 캠퍼스)가 경계에 반영되지 않음.  
→ 도로가 공원을 가로지를 때 기묘한 모양의 파티션, 빵꾸 뚫린 형태 발생.

**목표**:  
Overpass API로 OSM 실제 폴리곤을 가져와 **도로 등급에 따라 스마트하게 경계를 결정**,  
이쁘게 정리된 파티션으로 전면 교체. 동별로 순차 처리.

---

## DB 테이블/컬럼 변경 여부

| 테이블 | 변경 | 설명 |
|---|---|---|
| `world_partition` | **데이터만 교체** | 테이블 구조/컬럼 그대로 유지 |
| `world_partition_group` | **데이터만 교체** | 구조 그대로, 그룹 재정의 |
| `world_partition_group_member` | **전체 재생성** | regroup 스크립트로 재배정 |
| `world_area` | **변경 없음** | 동 경계 그대로 사용 |

**컬럼 추가 없음.** 기존 컬럼에 OSM 데이터 매핑:

| 기존 컬럼 | OSM 소스 |
|---|---|
| `boundary_geojson` | OSM feature polygon (실제 형태) |
| `source_layer` | OSM feature type (`osm_waterway`, `osm_park`, `osm_road` 등) |
| `source_feature` | OSM raw tags (`{ natural: water, name: 한강 }`) |
| `dominant_landuse` | OSM `landuse` / `natural` / `leisure` 태그에서 추론 |
| `theme_code` | landuse → theme 매핑 테이블 자동 할당 |
| `is_road` | `highway=*` 여부 |
| `area_m2` | Shapely 계산 |
| `centroid_lat/lng` | Shapely 계산 |

---

## 핵심: 도로 등급별 분할 전략

### 도로가 자연 지형을 가로지를 때 처리 원칙

```
도로 등급           분할 여부        이유
────────────────────────────────────────────────
motorway/trunk    ★ 반드시 분할    고속화도로 = 완전한 경계
primary           ★ 반드시 분할    간선도로 = 서울 동 경계 기준
secondary         ★ 반드시 분할    2차선 이상 = 실질 생활권 경계
tertiary          △ 조건부 분할    나머지 fragment ≥ 3,000m²이면 분할
residential       △ 조건부 분할    나머지 fragment ≥ 2,000m²이면 분할
service/footway   ✕ 분할 안 함    내부 동선 = 공원/캠퍼스 유지
path/track        ✕ 분할 안 함    등산로/산책로 = 지형 무시
```

### "빵꾸 뚫린" 형태 처리

```
문제: 공원 한가운데로 도로가 지나가면 
      park polygon - road polygon = 구멍 뚫린 도넛 or MultiPolygon 조각

처리:
  1. primary 이상 → difference() → MultiPolygon으로 분리 → 각각 별도 파티션
  2. tertiary/residential → fragment < threshold이면 다시 merge
  3. service/footway → 아예 차감하지 않음 (공원 내부 산책로는 공원 그대로)

최종 확인: area_m2 < MIN_AREA(500m²)인 조각은 인접 파티션에 흡수
```

### 시각적 결과 예시

```
[기존 방식]                    [신규 방식]
  공원         road            공원A    road    공원B
┌─────────┐  │     │         ┌──────┐  │   │  ┌────┐
│  ██████ │  │     │    →    │ ████ │  │   │  │████│
│  █ 빵꾸 │  │ 도로 │         │      │  │   │  │    │
│  ██████ │  │     │         └──────┘  │   │  └────┘
└─────────┘  │     │         (두 파티션으로 깔끔하게 분리)
```

---

## OSM 레이어별 파티션 생성 전략

### 처리 우선순위 (레이어 점유 순서)

```
1. waterway relation/way (한강, 안양천, 도림천) → 수계 먼저 확정
2. 동 경계(dong boundary)                      → 클리핑 마스크
3. major highway buffer (primary+)             → 주요 경계선 확정
4. landuse / natural / leisure 폴리곤           → 지형 단위 클리핑
5. secondary+ highway로 지형 세분화             → tertiary 조건부
6. 빈 공간                                     → residential 기본
```

### OSM 태그 → theme_code 매핑

| OSM 태그 | dominant_landuse | theme_code | source_layer |
|---|---|---|---|
| `natural=water` / `waterway=river` | water | `ancient_waterway` | `osm_waterway` |
| `leisure=park` | park | `sanctuary_green` | `osm_park` |
| `natural=wood` / `landuse=forest` | forest | `sanctuary_green` | `osm_forest` |
| `landuse=residential` | residential | `residential_zone` | `osm_landuse` |
| `landuse=commercial` | commercial | `urban_district` | `osm_landuse` |
| `amenity=school/university` | educational | `academy_sanctum` | `osm_amenity` |
| `amenity=hospital` | medical | `sanctuary_healing` | `osm_amenity` |
| `highway=*` (폴리곤) | road | `urban_road` | `osm_road` |
| `landuse=military` | military | `fortress_grounds` | `osm_landuse` |
| `landuse=industrial` | industrial | `forge_district` | `osm_landuse` |

---

## 실제 적용된 분할 규칙 (build_partitions_from_osm.py 구현 기준)

### 1. 레이어 처리 우선순위

OSM feature를 아래 순서대로 처리. 먼저 처리된 레이어가 영역을 점유하면 후순위 레이어는 그 영역을 제외하고 clip.

```
우선순위 1: osm_waterway  — natural=water, waterway=river/stream/canal
우선순위 2: osm_park      — leisure=park/garden/nature_reserve
우선순위 2: osm_forest    — natural=wood/scrub/heath, landuse=forest
우선순위 3: osm_education — amenity=school/university/college/kindergarten
우선순위 3: osm_medical   — amenity=hospital/clinic
우선순위 4: osm_landuse   — landuse=* (residential/commercial/industrial/military...)
우선순위 5: osm_feature   — 나머지 OSM 태그
우선순위 6: osm_fill      — OSM feature 없는 빈 공간 → residential 자동 채움
우선순위 7: osm_road      — primary+ 도로 buffer polygon
```

### 2. 도로 등급별 분할 결정

도로를 buffer로 팽창시켜 지형 폴리곤에서 차감(difference). 등급에 따라 buffer 크기와 적용 여부가 다름.

**무조건 분할 (primary+)**

| 도로 등급 | buffer 크기 | 실제 폭 |
|---|---|---|
| motorway / motorway_link | 0.000090도 | ~10m |
| trunk / trunk_link | 0.000080도 | ~9m |
| primary / primary_link | 0.000070도 | ~8m |
| secondary / secondary_link | 0.000055도 | ~6m |

→ 공원/숲이 이 등급 도로에 의해 잘리면 **무조건 두 개의 별도 파티션**으로 분리.

**조건부 분할 (tertiary / residential)**

| 도로 등급 | buffer 크기 | 분할 조건 |
|---|---|---|
| tertiary / tertiary_link | 0.000040도 | 분할 후 양쪽 조각이 모두 ≥ 2,000m² 일 때만 분할 |
| residential | 0.000030도 | 분할 후 양쪽 조각이 모두 ≥ 2,000m² 일 때만 분할 |

→ 작은 공원 안으로 골목길이 지나가도 조각이 2,000m² 미만이면 **분할 안 함** (빵꾸 방지).

**분할 안 함**

```
service, footway, path, track, cycleway, steps
→ 공원 내부 산책로/자전거도로/계단은 아예 고려 안 함
→ 공원/숲이 하나의 덩어리로 유지됨
```

### 3. 슬리버(잔여 조각) 처리

```
area_m2 < 500m² → 인접 파티션 중 경계 공유 길이가 가장 긴 파티션으로 흡수
반복 실행: 흡수 후 또 500m² 미만이 생기면 재실행 (수렴할 때까지)
```

### 4. 대형 파티션 자동 분할

```
비도로 파티션 area_m2 > 15,000m² → bbox 격자 분할 (목표 8,000m²)
도로 파티션 area_m2 > 30,000m²  → bbox 격자 분할 (목표 10,000m²)
분할 셀이 500m² 미만이면 버림
```

### 5. OSM 태그 → theme_code / dominant_landuse 매핑

| OSM 태그 | dominant_landuse | theme_code |
|---|---|---|
| natural=water, waterway=river/stream | water | ancient_waterway |
| leisure=park/garden, natural=grassland | park | sanctuary_green |
| natural=wood/scrub/heath, landuse=forest | forest | sanctuary_green |
| landuse=cemetery, grave_yard | cemetery | sanctuary_green |
| landuse=residential | residential | residential_zone |
| landuse=commercial, retail | commercial | urban_district |
| amenity=school/university/college | educational | academy_sanctum |
| amenity=hospital/clinic | medical | sanctuary_healing |
| landuse=industrial, railway | industrial | forge_district |
| landuse=military | military | fortress_grounds |
| osm_fill (빈 공간) | residential | residential_zone |
| osm_road (primary+ 도로) | road | urban_road |

### 6. 노량진1동 실행 결과 (2026-04-12)

| 항목 | 이전 | 이후 |
|---|---|---|
| 총 파티션 수 | 354개 | 268개 |
| 수계 (한강/안양천) | 0개 | 32개 |
| 공원 | grid 분할 | 27개 (실제 경계) |
| 숲 | grid 분할 | 6개 (실제 경계) |
| 교육시설 | 없음 | 4개 |
| 빈 공간 자동 채움 | — | 115개 |
| 최대 파티션 면적 | 286,721m² | 31,003m² |

### 7. 실행 커맨드 (재현용)

```bash
# OSM 데이터 수집 (캐시 저장)
python back/scripts/fetch_osm_features.py --dong-osm-id 3879474

# 파티션 재생성 (dry-run → 실행)
python back/scripts/build_partitions_from_osm.py --dong-osm-id 3879474 --dry-run
python back/scripts/build_partitions_from_osm.py --dong-osm-id 3879474

# 그룹 재배정
python back/scripts/regroup_partition_members.py --dong-osm-id 3879474

# 복원 시 (백업에서)
docker exec -i game-sogething-db psql -U game_sogething -d game_sogething \
  < back/world/data/backups/partition_backup_before_osm_rebuild_20260412.sql
```

---

## 백업 전략 (DB 덤프)

### 방법: Docker pg_dump

```bash
# world_partition 관련 3개 테이블 데이터 덤프
docker exec game-sogething-db pg_dump \
  -U game_sogething -d game_sogething \
  -t world_partition \
  -t world_partition_group \
  -t world_partition_group_member \
  --data-only --inserts \
  > back/world/data/backups/partition_backup_YYYYMMDD_HHMM.sql
```

- **형식**: SQL INSERT 문 (복원 시 psql로 직접 실행)
- **위치**: `back/world/data/backups/` (gitignore 대상)
- **복원**: `docker exec -i game-sogething-db psql -U game_sogething -d game_sogething < backup.sql`

기존 JSON 백업보다 완전한 복원 보장. 각 동 처리 전 반드시 실행.

---

## 할 일 체크리스트

### Phase 0 — 환경 확인 및 백업
- [ ] Docker pg_dump로 전체 파티션 백업 (`partition_backup_before_osm_rebuild.sql`)
- [ ] Overpass API 접근 테스트 (`https://overpass-api.de/api/interpreter`)
- [ ] 노량진1동 dong boundary_geojson 확인 (world_area 테이블)

### Phase 1 — OSM 데이터 수집 스크립트
- [ ] `back/scripts/fetch_osm_features.py` 작성
  - `--dong-osm-id` 파라미터
  - 동 경계 bbox 계산 (여유 0.005도 추가)
  - Overpass API 쿼리: waterway, landuse, natural, leisure, highway, amenity
  - osmium 없이 순수 JSON 파싱으로 GeoJSON 변환
  - 결과 캐시: `back/world/data/osm_cache/{dong_osm_id}_features.geojson`
  - rate limit 대비: 실패 시 재시도, 캐시 있으면 skip

### Phase 2 — OSM → 파티션 변환 스크립트
- [ ] `back/scripts/build_partitions_from_osm.py` 작성
  - 동 경계 polygon 로드
  - 레이어 우선순위대로 feature clip + 도로 등급별 분할 결정
  - 도로 교차 처리: `geom.difference(major_road_buffer)` → MultiPolygon 분리
  - fragment 병합: area < 500m² → 인접 파티션 흡수
  - area > 15,000m² → 자동 grid split (split_large_partitions 로직 재사용)
  - partition_key: `{dong_key}.osm.{layer}_{seq:04d}` 형식
  - `--dry-run` (통계 출력만)
  - `--dong-osm-id` 파라미터

### Phase 3 — 기존 파티션 교체 (동별)
- [ ] 해당 동 파티션 백업 (pg_dump --data-only)
- [ ] `world_partition_group_member` DELETE (해당 dong)
- [ ] `world_partition` DELETE (해당 dong)
- [ ] 새 파티션 INSERT
- [ ] `regroup_partition_members.py --dong-osm-id` 재실행

### Phase 4 — 노량진1동 적용 및 검증
- [ ] dry-run으로 결과 미리보기
- [ ] 파티션 수 / 수계 파티션 / 최대 면적 확인
- [ ] 실제 실행
- [ ] 인게임 확인 (경계선, 텍스처 흐름)

### Phase 5 — 노량진2동 동일 적용
- [ ] 같은 스크립트로 노량진2동 처리
- [ ] 검증

---

## 예상 결과 (노량진1동 기준)

| 지형 유형 | 예상 파티션 수 | 이전 대비 |
|---|---|---|
| 수계 (한강/안양천) | 5~10개 | 신규 |
| 공원/녹지 | 40~70개 | 더 자연스러운 경계 |
| 숲/자연 | 20~40개 | 산 능선 반영 |
| 주거 | 80~120개 | 도로 블록 단위 |
| 상업 | 20~40개 | 블록 단위 |
| 교육/의료 | 10~20개 | 캠퍼스 단위 |
| 도로 | 40~60개 | 현재 수준 |
| **합계** | **215~360개** | 현재 354개와 유사 |

---

## 주의사항

- Overpass API rate limit: 쿼리 사이 2-3초 sleep, 결과는 캐시 파일로 저장
- 한강 폴리곤은 동 경계 밖으로 크게 나감 → Shapely `intersection(dong_boundary)` clip 필수
- 일부 OSM feature는 way(라인)만 있고 폴리곤 없음 → 스킵
- OSM relation 타입(공원, 강 등)은 outer/inner ring 조합 → shapely MultiPolygon으로 변환
- 기존 `world_partition_group` 21개 정의는 유지 → regroup이 새 파티션에 자동 배정
- `world_area.boundary_geojson`이 노량진 동 경계를 갖고 있어야 함 (미확인 시 확인 필요)
- 복원 명령: `docker exec -i game-sogething-db psql -U game_sogething -d game_sogething < backup.sql`

---

## 관련 파일

| 파일 | 역할 |
|---|---|
| `back/scripts/fetch_osm_features.py` | 신규 — Overpass API 쿼리 + 캐시 |
| `back/scripts/build_partitions_from_osm.py` | 신규 — OSM → 파티션 변환 |
| `back/scripts/split_large_partitions.py` | 기존 — 대형 파티션 분할 로직 재사용 |
| `back/scripts/regroup_partition_members.py` | 기존 — 그룹 재배정 |
| `back/world/data/osm_cache/` | 신규 — Overpass 결과 캐시 (gitignore) |
| `back/world/data/backups/*.sql` | 신규 — pg_dump SQL 백업 (gitignore) |
| `back/world/models/models.py` | 변경 없음 |
