# 2026-04-12 작업 종합 계획

> 작성: 2026-04-12  
> 목적: 오늘 완료된 작업 정리 + 파티션 병렬 진행 로드맵

---

## 오늘 완료된 작업

### ✅ 성능 리팩토링 (전체 완료)

**프론트엔드**
- `useCurrentRegionInfo.js`: 25m 미만 이동 시 GPS state 갱신 생략
- `RpgWorld.jsx`: 동/구 감지 인터벌에 5m 이동 감지 추가 (정체 중 polygon 연산 스킵), 1500ms로 완화
- `RpgWorld.jsx`: `visibleMonsterElements` useMemo — `debugConfig.playerScale` 의존성 분리 (monsterScaleRef)
- `RpgWorld.jsx`: 파티션 데이터 단일 fetch (`sharedPartitions`) — `PartitionBoundaryOverlay` + `GroupColorOverlay` 공유
- `PartitionBoundaryOverlay.jsx`: 자체 fetch 제거, `partitions` prop 수신
- `GroupColorOverlay.jsx`: 자체 fetch 제거, `partitions` prop 수신
- `useAutoFarm.js`: `currentTargetRef` 추가 — 타겟 유효 시 전체 몬스터 순회 스킵 (O(n) → O(1))

**백엔드**
- `MonsterManager.py`: `_normal_count` 캐시 (스폰/삭제 시점에만 갱신), `math.sqrt()` → distSq 비교
- `MonsterManager.py`: Grid Bucket 공간 인덱싱 (`_GRID_CELL=30m`) — O(n×m) → O(n + m×k)
- `partition_service.py`: `get_current_region_info` 이중 DB 쿼리 제거, Shapely `_shape_cache` 추가
- `router.py`: 몬스터 sync 임계값 15m→25m, 2s→3s

### ✅ 노량진1동 OSM 기반 파티션 재생성 완료

| 항목 | 이전 | 이후 |
|---|---|---|
| 총 파티션 수 | 354개 | 268개 |
| 수계(한강/안양천) | 0개 | 32개 |
| 공원 | grid 분할 | 27개 (실제 경계) |
| 최대 파티션 면적 | 286,721m² | 31,003m² |

### ✅ 바닥 텍스처 그룹 단위 렌더링 계획 수립

- `1600_바닥_텍스처_그룹_단위_렌더링.md` 작성 완료
- `usePartitionTexture` 플래그 `RpgWorld.jsx`에 추가됨
- 구현 방향: **안 A (프론트 전용)** — `CityBlockOverlay`에 그룹 경계 렌더링 추가

---

## 현재 진행 중인 메인 작업

### 🔨 월드 파티션 구역 확장 (메인)

```
노량진1동 ✅ 완료
노량진2동 ⏳ 대기
기타 동들 📋 예정
```

**파티션 작업 순서 (동별)**
1. 백업 (`docker exec game-sogething-db pg_dump ...`)
2. OSM 데이터 수집 (`fetch_osm_features.py --dong-osm-id {id}`)
3. 파티션 재생성 (`build_partitions_from_osm.py --dong-osm-id {id}`)
4. 그룹 재배정 (`regroup_partition_members.py --dong-osm-id {id}`)
5. 인게임 확인

---

## 병렬 구현 가능한 작업 목록

> 파티션 작업과 독립적으로 진행 가능. 우선순위 순.

### 🅐 파티션 완성 직후 즉시 활용되는 기반 (지금 만들어두면 좋음)

#### A1. 몬스터 스폰 파티션 기반 인터페이스 준비
- **파일**: `back/monster/managers/MonsterManager.py`
- **할 것**: `spawn_for_partition(partition_key, group_key, center_lat, center_lng)` 메서드 추가
- 현재 하드코딩된 20개 좌표를 DB partition 좌표로 교체할 준비
- **난이도**: 낮음 | **효과**: 파티션 완성 즉시 구역별 몬스터 배치 가능
- **체크리스트**:
  - [ ] `spawn_for_partition()` 메서드 추가 (현재 `_spawn_all_at_start` 분리)
  - [ ] partition_key 기준 스폰 좌표 계산 (centroid_lat/lng 활용)
  - [ ] 기존 하드코딩 스폰은 fallback으로 유지

#### A2. NPC 엔티티 기반
- **파일**: `front/src/entity/world/Portal.jsx` → NPC 컴포넌트로 확장, 백엔드 NPC 테이블 추가
- **할 것**: NPC 3D 오브젝트 + 클릭 시 대화창 UI + DB `world_npc` 테이블
- 파티션에 NPC 배치 시 좌표 연결만 하면 바로 동작
- **난이도**: 중간 | **효과**: 파티션 완성 후 마을/상인 즉시 배치 가능
- **체크리스트**:
  - [ ] `world_npc` 테이블 설계 (id, partition_key, name, role, dialogue_json, lat, lng)
  - [ ] `NpcEntity.jsx` — 3D 오브젝트 + 이름표
  - [ ] `NpcDialogue.jsx` — 대화창 UI (말풍선 or 전체화면 패널)
  - [ ] `/api/world/dong/:id/npcs` 엔드포인트

---

### 🅑 파티션 무관, 즉시 게임성 향상

#### B1. 장비 스탯 전투 실제 반영 확인
- **파일**: `back/player/services/service.py`, `back/player/managers/PlayerManager.py`
- **현황**: `handle_hit`에서 `player_stats.get("attack", 10)` 사용. 장착 장비 스탯이 이 값에 합산되는지 확인 필요
- **난이도**: 낮음 (확인 후 수정 30분 이내)
- **체크리스트**:
  - [ ] `PlayerManager.get_player_stats()`에서 장비 스탯 합산 여부 확인
  - [ ] 미반영 시 equipment 스탯 합산 로직 추가
  - [ ] 프론트 StatusPanel에서 공격력 수치 표시 확인

#### B2. 스킬 쿨다운 + 스킬 다양화
- **파일**: `front/src/entity/player/logic/usePlayerSkills.js`, `front/src/hooks/useAutoAttack.js`
- **현황**: R키 무한 발사, 스킬 2개(pyramid_punch, magic_orb)만 존재
- **난이도**: 낮음~중간
- **체크리스트**:
  - [ ] `usePlayerSkills.js`: `cooldownRef` 추가, 마지막 사용 시각 기록
  - [ ] Q/E/R 키별 스킬 분리 (Q=펀치, E=마법구체, R=예비 슬롯)
  - [ ] 쿨다운 UI: 스킬 아이콘 위 쿨다운 오버레이 (StatusPanel 하단)
  - [ ] 백엔드 `SKILL_POWER`에 쿨다운 시간 추가

#### B3. 레벨업/사망 연출 UI
- **파일**: `front/src/engine/useGameSocket.js`, `front/src/ui/GameOverlay.jsx`
- **현황**: `player_reward` 메시지에 `leveledUp: true` 있지만 화면 효과 없음. 사망 시 HP만 즉시 회복
- **난이도**: 낮음
- **체크리스트**:
  - [ ] `LevelUpEffect.jsx`: 화면 빛 폭발 + "LEVEL UP!" 3D 텍스트 (DamageNumber 방식)
  - [ ] `DeathRespawnEffect.jsx`: 사망 시 화면 페이드아웃 → 0.5초 정지 → 원점 이동 → 페이드인
  - [ ] useGameSocket에서 `player_reward.leveledUp` 수신 → 이펙트 트리거
  - [ ] useGameSocket에서 `player_hit` → HP 0 → 사망 연출 트리거

#### B4. 임시 상점 UI (gold 소비처)
- **파일**: 신규 `front/src/ui/ShopModal.jsx`, `back/item/routers/router.py`
- **현황**: gold를 쓸 곳이 없어서 경제 테스트 불가
- **난이도**: 중간
- **체크리스트**:
  - [ ] 상점 아이템 목록 API (`/api/item/shop`)
  - [ ] `ShopModal.jsx` — 아이템 목록 + gold로 구매 버튼
  - [ ] 구매 시 gold 차감 + 인벤토리 추가
  - [ ] GameOverlay에 상점 버튼 추가

---

## 전체 로드맵

```
2026-04-12 오늘
  ├─ ✅ 성능 리팩토링 완료
  ├─ ✅ 노량진1동 OSM 파티션 완료
  └─ 🔨 바닥 그룹 렌더링 진행 중

가까운 미래 (파티션 병렬)
  ├─ B1 장비 스탯 반영 확인 (30분)
  ├─ B2 스킬 쿨다운 (2-3시간)
  ├─ B3 레벨업/사망 연출 (2시간)
  └─ 노량진2동 파티션 재생성

파티션 어느 정도 완성 후
  ├─ A1 몬스터 스폰 파티션 연동
  ├─ A2 NPC 시스템 기반
  └─ B4 상점 UI

파티션 전체 완성 후
  ├─ 퀘스트/스토리 시스템
  ├─ 파티션별 몬스터/아이템 테이블
  └─ NPC 대화 + 스토리 연결
```

---

## 주의사항

- `world_partition` micro 파티션 **절대 삭제 금지** — 위치 판정, NPC 배치 모두 여기 의존
- WebSocket 메시지 타입 변경 시 프론트/백엔드 **동시 수정** 필수
- 동별 파티션 작업 전 반드시 **pg_dump 백업** 실행
- 장비 스탯 반영 수정 시 기존 저장된 캐릭터 스탯과 충돌 여부 확인
