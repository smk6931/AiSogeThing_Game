---
description: Keep project coding style rules in `agents/` and use this skill only as a reminder to avoid duplicating them.
---

# Coding Standards Helper

- 프로젝트 코딩 스타일과 구조 규칙은 `agents/`가 기준이다.
- 이 스킬은 강한 프로젝트 취향을 전역처럼 다시 정의하지 않는다.
- 스타일 규칙이 필요하면 관련 `agents/` 문서를 먼저 보고, 새 공통 규칙이 필요하면 `agents/`에 추가한다.
