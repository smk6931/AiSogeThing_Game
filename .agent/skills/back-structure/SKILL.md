---
name: 백엔드 구조 보조 스킬
description: 백엔드 구조 규칙은 `agents/`를 우선 참조하고, 이 스킬은 중복 정의를 하지 않습니다.
---

# 백엔드 구조 보조 스킬

- `back/` 구조 규칙은 프로젝트 `agents/` 문서가 기준이다.
- 새 도메인 추가, 레이어 분리, 파일 위치 판단은 관련 `agents/` 문서를 먼저 읽는다.
- 이 스킬에는 프로젝트 구조 본문을 중복 작성하지 않는다.
