---
name: API 보조 스킬
description: 프로젝트 API 규칙은 `agents/`를 우선 참조하고, 이 스킬은 그 진입만 안내합니다.
---

# API 보조 스킬

- 프로젝트 API 규칙의 source of truth는 `AGENTS.md`와 `agents/`다.
- API 경로, 정적 파일 경로, 라우터 규칙을 바꾸거나 확인할 때는 먼저 관련 `agents/` 문서를 읽는다.
- 이 스킬에는 프로젝트 API 규칙 본문을 중복 작성하지 않는다.
