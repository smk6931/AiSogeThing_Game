---
name: 프론트 구조 보조 스킬
description: 프론트 구조 규칙은 프로젝트 `agents/`를 우선 참조하고, 이 스킬은 중복 정의를 하지 않습니다.
---

# 프론트 구조 보조 스킬

- `front/src/` 구조 규칙은 프로젝트 `agents/` 문서가 기준이다.
- 새 폴더 추가, UI 구조 변경, 엔진과 엔티티 분리 판단은 관련 `agents/` 문서를 먼저 읽는다.
