[오라클 클라우드 서버 설정 정보 (Oracle Cloud)]

1. 인스턴스 사양 (일명 괴물 스펙)
   이름: instance-20260117-1446
   형태: VM.Standard.A1.Flex (암페어 알트라 ARM 프로세서)
   CPU: 4 코어 (OCPU)
   램(RAM): 24 GB
   네트워크 속도: 4 Gbps
   운영체제: 우분투 24.04 LTS (Canonical Ubuntu)
   아키텍처: aarch64 (ARM64 구조)
   생성일: 2026-01-17

2. 저장공간 (스토리지)
   부트 볼륨: 200 GB
   성능: 10 VPU (밸런스 모드)
   암호화: 전송 중 암호화 적용됨

3. 네트워크 설정
   VCN 이름: SonVnc
   서브넷: public subnet-SonVnc (공개망)
   공인 IP: 활성화됨 (오라클 콘솔에서 IP 숫자 확인 필요)
   DNS 레코드: 예

4. 보안 및 접근 권한
   SSH 키: 등록됨 (ssh-rsa AAAAB3...)
   클라우드 가드: 활성화됨
   취약점 스캔: 비활성화됨

5. 배포 시 주의사항 (필독)
   - 이 서버는 ARM 아키텍처입니다. (애플 M1/M2/M3와 같은 구조)
   - 도커(Docker) 이미지 만들 때 linux/arm64 호환되는지 확인해야 합니다. (우리가 쓰는 파이썬, 노드는 대부분 문제없습니다).
   - 오라클 클라우드는 처음에 포트가 다 막혀있습니다. 서버 접속 후 인터넷 포트(80, 443)를 여는 명령어를 따로 쳐줘야 접속이 됩니다.
