2026-02-05

===
오후 (작업 마무리)
1. 초기 진입 화면 개편 (PlatformEntry 구현)
   - 기존의 단조로운 홈 화면 진입 대신, '소개띵'과 '게임'을 선택할 수 있는 프리미엄 진입 화면 추가.
   - 사용자 요청에 따라 고퀄리티 배경 이미지(/images/7b27...jpg) 적용.
   - 글래스모피즘(Glassmorphism) 및 애니메이션 효과를 적용하여 세련된 UI 구현.
   - 모바일 중심의 반응형 레이아웃 적용.
2. 인증 연동 및 라우팅 수정
   - 로그인 성공 시 바로 /entry 경로로 리다이렉트되도록 수정.
   - 앱 기본 경로(/) 접근 시 /entry로 이동하도록 설정.
   - Login.jsx에서 AuthContext의 login 함수 인자(email, password) 불일치 문제 해결.

================================================================================
추가 업무 기록: 리액트 vs 게임 엔진 렌더링 최적화
================================================================================

1. 기술 분석: React 상태 관리 vs Game Engine 최적화
   - 주제: 왜 3D 게임 개발에서 useState 대신 useRef를 사용하는가?
   
   - useState (표준 웹 방식)
     * 값이 변경될 때마다 React가 가상 DOM을 비교하여 화면 전체를 재렌더링함.
     * 초당 60회 이상 발생하는 프레임 업데이트(Tick)에 사용 시 과부하 및 렉(Lag) 유발.
     
   - useRef (고속 게임 방식)
     * React의 렌더링 감시망을 우회하여 값을 직접 수정함.
     * React는 값이 변한 줄 모르지만, Three.js(GPU)는 프레임마다 이 값을 직접 읽어감.
     * 결과적으로 CPU 부하를 최소화하면서 매끄러운 60FPS 이동 구현 가능.

2. 구현 사항: 피라미드 펀치 (1단계 분열 로직)
   - 구현 내용: 
     * Unreal 스타일의 Tick 시스템(delta 누적) 적용.
     * 발사 후 1.0초 시점에 현재 위치를 기점으로 +/- 45도 방향 분신 2개 생성.
     * 본체는 방향을 꺾지 않고 원래 속도대로 직진 유지.
     * generation 변수를 통해 자식이 무한 증식하는 것을 방지.

3. 향후 계획
   - 현재 왼쪽 발사체(side:left) 기반으로 로직 완성 후 오른쪽 대칭 적용 예정.
   - 분열 시 펀치들이 겹치지 않게 수직 오프셋(Side Shift) 추가 고려 중.

================================================================================
추가 업무 기록: 프랙탈 피라미드 펀치 로직 종결
================================================================================

1. 구현 사항: 제너릭 기하급수 분열 (Recursive Split)
   - 반복적인 if-else 구문을 제거하고 generation 변수를 활용한 통합 로직 구축.
   - 1 -> 2 -> 4 -> 8 -> 16발(4세대)까지 자동으로 증식하도록 구현.
   - baseAng Prop을 통한 '가문 각도 계승' 성공: 모든 자식이 최초 발사 방향을 기준으로 정렬됨.
   - 분열 폭(spreadOffset) 자동 조절: 세대가 올라갈수록 각도를 1/2씩 좁혀 겹침 현상 방지.

2. 기술적 성과
   - "드디어 시팔 되었네" 단계 도달: 사용자 의도에 맞는 웅장한 부채꼴 대열 완성.
   - 4.0초간의 빌드업 후 16발의 황금 펀치가 화면을 가득 채우는 연출 성공.

3. 향후 계획
   - 피라미드 대열의 시각적 완성도를 위해 각 펀치별 소폭의 위치 오프셋(Z-Depth) 조정 검토.

================================================================================

================================================================================
